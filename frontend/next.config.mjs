/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  typescript: {
    // We will verify all TypeScript errors
    ignoreBuildErrors: false,
  },
};

export default nextConfig;
